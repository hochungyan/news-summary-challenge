export default class NewsItemModel {
    constructor(webTitle, webPublicationDate, webUrl, thumbnail) {
        this.webTitle = webTitle;
        this.webPublicationDate = webPublicationDate;
        this.webUrl = webUrl;
        this.thumbnail = thumbnail;
    }
}